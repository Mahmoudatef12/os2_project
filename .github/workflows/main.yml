on:
 push:
   branches:
     - master
  



jobs:
  build:
    runs-on: ubuntu-latest

    steps: 
      - name: "github actions"
        run: ls 

      - uses: actions/checkout@v3
      
      - name : "docker login"
        run : docker login --username mahmoudatefriad --password dckr_pat_pRE9lSxelm11IdYMK9DwS8Wv9IE

